# macOS
.DS_Store
**/.DS_Store

# Vercel
.vercel

# Editors
.vscode
.idea

# Raw working assets (not referenced by site)
LHFF STILLS EDITED/
studio one /
app/
DJI_*.MP4
*.mov
*.MOV

# Remotion build artifacts (node_modules covered below; MP4s ship from social/*/motion/).
# Remotion SOURCE stays tracked so any drop can be re-rendered; only artifacts are ignored.
social/*/remotion/out/
social/*/remotion/.remotion/
# Regenerable image copies (originals live in each drop's ai-assets/)
social/*/remotion/public/

# Logs
*.log
npm-debug.log*

# Node
node_modules/
